<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="rehab.reality.alsuti.UploadActivity"
    tools:showIn="@layout/activity_upload">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Encrypt + Upload"
        android:id="@+id/encryptUploadButton"
        android:onClick="onEncUploadBtn"
        android:nestedScrollingEnabled="false"
        android:layout_below="@+id/rawUploadButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Upload"
        android:id="@+id/rawUploadButton"
        android:onClick="onRawUploadBtn"
        android:nestedScrollingEnabled="false"
        android:layout_below="@+id/password"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/editText"
        android:text="Uploading..."
        android:inputType="none"
        android:textIsSelectable="true"
        android:visibility="invisible"
        android:layout_alignBaseline="@+id/rawUploadButton"
        android:layout_alignBottom="@+id/rawUploadButton"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
